- name: Install and ensure NGINX is running
  hosts: web
  become: yes
  tasks:
  - name: Install NGINX package
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start and enable NGINX service
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes